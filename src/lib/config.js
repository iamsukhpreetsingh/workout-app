// Backend base URL. For the Android emulator use http://10.0.2.2:4000
// (alias for host loopback); for iOS simulator http://localhost:4000; for a
// real device on the same Wi-Fi, use your computer's LAN IP.
export const API_URL = process.env.EXPO_PUBLIC_API_URL || 'http://13.126.205.202:4000';
