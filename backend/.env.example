DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/workout_tracker
PGSSL=false
JWT_SECRET=replace-with-a-long-random-string
PORT=4000

# Cloud provider swap (Supabase / Railway / Neon) — same app, config only:
# DATABASE_URL=postgres://user:pass@host.supabase.co:5432/postgres
# PGSSL=true
